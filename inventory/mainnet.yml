---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    near:
      hosts:
        ts.validator.near.mainnet.encapsulate.xyz:
          type: validator
